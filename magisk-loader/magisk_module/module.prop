id=${moduleId}
name=${api} - LSPosed Next
version=${versionName} (${versionCode})
versionCode=${versionCode}
author=${authorList}
description=LSPosed Next: updated Xposed-compatible framework. Supports Android 8.1 through the latest releases (tested to Android 16/17) with KernelSU Next compatibility. ${requirement}.
updateJson=${updateJson}
